# Test Environment Variables for Playwright E2E Tests
# =====================================================

# NextAuth Configuration (Required for test server to start)
NEXTAUTH_SECRET=test-secret-key-for-playwright-testing-environment-minimum-32-characters
NEXTAUTH_URL=http://localhost:3000

# Basic Configuration
NODE_ENV=test
NEXT_PUBLIC_APP_ENV=test
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Mock Database URL (not actually used in tests)
DATABASE_URL=postgresql://test:test@localhost:5432/test

# Mock OAuth (not actually used in tests)
GOOGLE_CLIENT_ID=test-google-client-id
GOOGLE_CLIENT_SECRET=test-google-client-secret